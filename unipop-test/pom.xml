<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>unipop</artifactId>
        <groupId>com.github.unipop-graph</groupId>
        <version>0.2.2-SNAPSHOT</version>
    </parent>
    <artifactId>unipop-test</artifactId>
    <name>Unipop :: Integration tests</name>
    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.1</version>
            <scope>test</scope>
        </dependency>
        <!-- unipop-elastic compile jar (ES client, controllers) -->
        <dependency>
            <groupId>com.github.unipop-graph</groupId>
            <artifactId>unipop-elastic</artifactId>
            <version>${project.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>junit</groupId>
                    <artifactId>junit</artifactId>
                </exclusion>
            </exclusions>
            <scope>test</scope>
        </dependency>
        <!-- unipop-elastic test-jar (EmbeddedElasticsearchServer + ElasticGraphProvider) -->
        <dependency>
            <groupId>com.github.unipop-graph</groupId>
            <artifactId>unipop-elastic</artifactId>
            <version>${project.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
        <!-- unipop-jdbc compile jar (jdbc controllers, jOOQ, embedded-postgres) -->
        <dependency>
            <groupId>com.github.unipop-graph</groupId>
            <artifactId>unipop-jdbc</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- unipop-jdbc test-jar (EmbeddedPostgresServer + JdbcGraphProvider) -->
        <dependency>
            <groupId>com.github.unipop-graph</groupId>
            <artifactId>unipop-jdbc</artifactId>
            <version>${project.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
        <!-- Embedded Postgres harness for the jdbc side of federation. These are test-scoped in
             unipop-jdbc, so they do NOT arrive transitively (test-scope deps never transit) and the
             test-jar carries no deps of its own — re-declare them here. Binaries are darwin-arm64
             (mirrors unipop-jdbc); other platforms need the matching embedded-postgres-binaries-* . -->
        <dependency>
            <groupId>io.zonky.test</groupId>
            <artifactId>embedded-postgres</artifactId>
            <version>2.0.7</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.zonky.test.postgres</groupId>
            <artifactId>embedded-postgres-binaries-darwin-arm64v8</artifactId>
            <version>16.4.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>42.7.4</version>
            <scope>test</scope>
        </dependency>
        <!-- Testcontainers (Docker lifecycle + ES module) -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <version>1.20.4</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>elasticsearch</artifactId>
            <version>1.20.4</version>
            <scope>test</scope>
        </dependency>
        <!-- Guice DI (Cucumber-Guice IoC) -->
        <dependency>
            <groupId>com.google.inject</groupId>
            <artifactId>guice</artifactId>
            <version>4.2.3</version>
            <scope>test</scope>
        </dependency>
        <!-- Cucumber -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-java</artifactId>
            <version>7.21.1</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-junit</artifactId>
            <version>7.21.1</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-guice</artifactId>
            <version>7.21.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <testResources>
            <!-- Cucumber ObjectFactory SPI -->
            <testResource>
                <directory>test-resources</directory>
            </testResource>
            <!-- Provider config JSONs (/configuration/**) the graph providers resolve at test time -->
            <testResource>
                <directory>resources</directory>
            </testResource>
        </testResources>
        <directory>${basedir}/target</directory>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>17</release>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <includes>
                            <!-- IntegFeatureTest (federated Gherkin) + MultiFieldTests (elastic
                                 multi-field) complete against live ES 8 + embedded Postgres at a
                                 partial-federation parity (see #143 Phase-4 PR). IntegStructureSuite
                                 is intentionally NOT in the default run: like RestStructureSuite it
                                 hangs on the GraphMigrator IO tests — the partial federation provider
                                 throws mid-migration, the GraphML piped writer thread dies and the
                                 reader deadlocks on PipedInputStream. Tracked as a Phase-4 follow-up. -->
                            <include>**/IntegFeatureTest.java</include>
                            <include>**/MultiFieldTests.java</include>
                        </includes>
                        <environmentVariables>
                            <!-- Docker Engine >= API 1.40 (Docker Desktop 4.78 / engine 29.x)
                                 rejects requests carrying a pre-1.40 API version with HTTP 400.
                                 The docker-java config shaded into Testcontainers 1.20.4 otherwise
                                 negotiates from an older default, so its initial /info probe is
                                 rejected and "Could not find a valid Docker environment" is thrown.
                                 We pin api.version (argLine system property) >= 1.40, point at the
                                 active Docker Desktop socket explicitly, and disable Ryuk (the
                                 per-JVM container is torn down via a JVM shutdown hook). -->
                            <DOCKER_HOST>unix://${user.home}/.docker/run/docker.sock</DOCKER_HOST>
                            <TESTCONTAINERS_RYUK_DISABLED>true</TESTCONTAINERS_RYUK_DISABLED>
                        </environmentVariables>
                        <argLine>-Dapi.version=1.41 --add-opens java.base/java.lang=ALL-UNNAMED --add-opens java.base/java.lang.invoke=ALL-UNNAMED --add-opens java.base/java.lang.reflect=ALL-UNNAMED --add-opens java.base/java.util=ALL-UNNAMED --add-opens java.base/java.util.concurrent=ALL-UNNAMED --add-opens java.base/java.util.concurrent.atomic=ALL-UNNAMED --add-opens java.base/java.io=ALL-UNNAMED --add-opens java.base/java.nio=ALL-UNNAMED --add-opens java.base/sun.nio.ch=ALL-UNNAMED</argLine>
                        <testFailureIgnore>true</testFailureIgnore>
                        <trimStackTrace>false</trimStackTrace>
                        <reuseForks>false</reuseForks>
                        <forkedProcessTimeoutInSeconds>1200</forkedProcessTimeoutInSeconds>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>install</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/lib</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
